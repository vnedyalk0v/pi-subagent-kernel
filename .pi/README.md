# `.pi/` examples

This directory contains Pi-related examples only.

Do not create or modify active `.pi/settings.json` as a convenience. Project settings can change which packages, extensions, skills, prompts, and themes Pi loads. Treat active settings changes as owner-approved repository/harness behavior changes.

Use `.pi/settings.example.json` only as a reference if the owner asks for Pi settings guidance.
